<h1><%= t("editing_service_contract") %></h1>
<%= error_messages_for :service_contract %>
<% form_for(@service_contract, :url => object_url, :html => { :method => :put }) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%=submit_tag t("update")%>
  </p>
<% end %>
